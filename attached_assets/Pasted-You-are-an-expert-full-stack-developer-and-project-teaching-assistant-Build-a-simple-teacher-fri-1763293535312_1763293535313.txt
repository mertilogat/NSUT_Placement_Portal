You are an expert full-stack developer and project-teaching assistant. Build a **simple, teacher-friendly, production-demonstrable** Internship/Placement Portal for NSUT as a DBMS project.

**CRITICAL**: Before writing any code, list clarifying questions and suggestions you need and wait for my answers. Do not start coding until I reply.

Primary constraints (must follow)
- **Database:** MySQL only. Provide a single SQL file `db/nsut_placement.sql` containing only basic SQL commands that we were taught: `CREATE`, `INSERT`, `UPDATE`, `DELETE`, `SELECT`, `JOIN`, `ALTER`. Use explicit, simple SQL — **no ORMs, no migrations frameworks**, no advanced SQL features the teacher did not teach.
- **Environment (how the teacher will run it):** Designed to run locally on **XAMPP (Apache + MySQL)**. Provide step-by-step instructions for importing SQL via **phpMyAdmin** or `mysql` CLI.
- **Backend:** **Python (Flask)** — but keep it minimal and simple. Use direct SQL queries executed via a light MySQL connector library (e.g., `mysql-connector-python` or `PyMySQL`). **Do not** use SQLAlchemy or any ORM. Keep code readable and comment every SQL statement for viva explanation.
- **Frontend:** Plain HTML/CSS with optional **Bootstrap** and minimal JavaScript. No React, Vite, TypeScript, or other heavy frontend frameworks.
- **Auth:** Session-based authentication using Flask sessions. Passwords hashed via `bcrypt` (`werkzeug.security` or `bcrypt` lib). Role-based access control (Student, PC Admin, Company Recruiter — simple checks in code).
- **File uploads:** Resume upload (PDF only) saved to server filesystem; filename stored in DB. Validate file type and size (configurable).
- **Testing & README:** Provide a clear README (teacher-friendly) with exact local run steps. Include a short manual test checklist and how to explain project design in viva.
- **Simplicity rule:** Use only technologies/methods that are reasonable to explain in class. Show plain SQL files and simple queries.

Minimum functional requirements (simple MVP)
1. Users & Roles
   - Student signup/login/profile (fields we will confirm).
   - PC Admin created via seed SQL or initial admin script.
   - Company Recruiter signup with admin activation.
2. Jobs/Postings
   - Companies add job posts (title, description, type, min CGPA, allowed branches, deadline, openings).
   - Admin approve/reject postings.
3. Applications
   - Students browse and apply (attach resume), application status tracked (Applied → Shortlisted → Interview → Selected/Rejected).
   - Admin/Company update statuses.
4. Interview slots (simple)
   - Create interview events (date/time, location/link). Invite shortlisted students. Students can accept/decline.
5. Placement cell features
   - Simple dashboard counts (companies, openings, applications, placed).
   - CSV bulk student import script (simple parsing & INSERTs).
   - Announcements board by PC Admin.
6. Search & filters
   - Text search on job title/company and filters on CGPA/branch/year.

Deliverables (teacher-friendly)
- `db/nsut_placement.sql` — full schema and sample seed `INSERT`s (~10 students, 3 companies, 5 jobs).
- `backend/` — Flask app using basic structure (`app.py`, `db.py` (connection helper), `auth.py`, `student.py`, `company.py`, `admin.py`, `uploads/`).
- `frontend/` or templates inside Flask — simple Bootstrap pages and forms.
- `README.md` — high-quality, clear, step-by-step, containing:
  1. How to install XAMPP and start Apache & MySQL.
  2. How to import `db/nsut_placement.sql` via phpMyAdmin or `mysql` CLI.
  3. How to set environment variables or an `.env.example`.
  4. How to run the Flask backend (virtualenv, `pip install -r requirements.txt`, `python app.py`) and open in browser.
  5. How to seed the admin account and sample data (SQL or script).
  6. How to run basic manual tests (test checklist).
  7. How to export project from Replit (download ZIP / push to GitHub) and steps to run on local machine.
- ER diagram PNG/SVG + short explanation file.
- Simple scripts: `run_local.sh` or commands for Windows (batch) to start everything.
- Manual test cases and checklist (for teacher to run).
- Well-commented code (for viva).

Security & exam-safety
- Use prepared statements for all SQL executed from Python to avoid injection (but keep syntax simple and readable).
- Validate file uploads (only PDF, max size).
- Do not integrate services the teacher didn’t teach (no OAuth, no cloud DB providers, no ORMs).
- Keep everything explainable in plain SQL.

Before coding — mandatory clarifying questions to ask me (you must ask these and wait for answers)
1. Do we use **Flask (Python)** for backend (I prefer Flask) — confirm. (Note: this prompt already assumes Flask; ask if they want PHP instead).
2. Approve exact student fields to store. I propose: `id, name, roll_no, email, password_hash, branch, year, cgpa, phone, skills_text, resume_filename, created_at`.
3. Confirm branch list and years (e.g., branches: CSE, ECE, ME, CE; years: 1,2,3,4).
4. Approve resume policy: PDF only, max 2 MB (adjustable).
5. Company signup flow: self-register + admin approval or admin-only creation?
6. Any NSUT-specific eligibility rules to enforce automatically (e.g., internships min CGPA 6.0)? If none, keep eligibility per-job.
7. CSV format for bulk student upload: confirm columns (I propose: roll_no,name,email,branch,year,cgpa,phone,skills).
8. UI theme preference (Bootstrap default) and whether to include an NSUT logo (if provided).
9. Which Python MySQL library do you prefer? I recommend `mysql-connector-python` (simple, official). `PyMySQL` also ok.
10. Are you required to run everything under XAMPP (i.e., teacher will run Apache+MySQL) and run Flask separately? (I will provide steps for both Windows & Linux).
11. Do you want a short `viva_notes.md` explaining design and 5 sample SQL queries to show the teacher? (I recommend yes.)

Implementation & structure expectations (simple)
- Use plain SQL in `db/nsut_placement.sql`. Provide sample `INSERT`s to create admin and seed data.
- Use a `db.py` to open/close DB connections (using `mysql.connector.connect`) and a small `queries.py` to centralize SQL strings (commented).
- Use Flask sessions (server-side) for authentication.
- Keep frontend templates in `templates/` and static CSS/JS in `static/`.
- Provide `requirements.txt` with only necessary libraries (`Flask`, `mysql-connector-python`, `python-dotenv`, `bcrypt` or `werkzeug`).
- Provide a clear `README.md` (teacher-focused).

When you code:
- First produce the `db/nsut_placement.sql` and ER diagram.
- Then produce backend files and frontend templates in logical chunks. Show meaningful commit-like headers before each chunk.
- Provide exact commands to run locally (XAMPP + Flask) and to import SQL.
- If anything cannot be done in Replit due to limitations, explicitly explain alternative.

Now: list your clarifying questions and suggestions (use the exact 11 questions above as a checklist). Wait for my answers before writing any code.